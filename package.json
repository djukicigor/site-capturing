{
  "name": "site-capturing",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --once --driver-package meteortesting:mocha"
  },
  "dependencies": {
    "@babel/runtime": "^7.0.0",
    "fibers": "^3.0.0",
    "meteor-node-stubs": "^0.4.1",
    "puppeteer": "^1.9.0",
    "simpl-schema": "^1.5.3"
  },
  "devDependencies": {
    "chai": "^4.1.2"
  }
}
